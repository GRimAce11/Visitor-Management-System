const VisitorData = (id, vname, vcompName, vdate, vtime, vmobile) => {
    const data = {
        vid: id,
        vname: vname,
        vcompName: vcompName,
        vdate: vdate,
        vtime: vtime,
        vmobile: vmobile,
    }
    return data
}

export default VisitorData